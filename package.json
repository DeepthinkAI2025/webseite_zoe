{
  "name": "zoe-monorepo-root",
  "private": true,
  "scripts": {
    "dev": "cd next-app && npm run dev",
    "build": "cd next-app && npm run build",
    "start": "cd next-app && npm start",
    "lint": "cd next-app && npm run lint || echo 'lint optional (ignoring errors)'"
  },
  "devDependencies": {
    "@babel/parser": "^7.28.3",
    "@babel/traverse": "^7.28.3"
  }
}
