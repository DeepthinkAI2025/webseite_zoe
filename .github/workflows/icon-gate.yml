name: icon-gate
on:
  pull_request:

jobs:
  icon-barrel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install app
        working-directory: next-app
        run: npm ci --no-audit --no-fund || npm install --no-fund --no-audit
      - name: Icon Gate (placeholder)
        working-directory: next-app
        run: echo "Icon Gate placeholder - implement scripts/seo/icon-gate later" 
